# LOCKED MODE: Laguna S-2.1 sole resident — PROD cutover 2026-07-22.
# Exclusive occupancy (~92 GB at util 0.85): KV pool 858k tokens = 3.28x
# concurrent full-256k sessions. Nothing else fits alongside; vision lane is
# DARK while this config is live (restore llama-swap.yaml.bak.20260722-prelaguna
# for the nemotron+vision resident pair, llama-swap.full.bak for full roster).
# Old prod names (qwen3.6-27b, qwen3.6-35b-a3b, nemotron-3-puzzle-75b) are
# remapped to laguna at the LiteLLM GATEWAY (llama-swap aliases don't route —
# see memory feedback_llama_swap_alias_reload).

healthCheckTimeout: 1200
logLevel: info
models:
  laguna-s-2.1:
    cmd: /home/max/llm-stack/bin/launch-vllm-laguna-s21-nvfp4.sh
    proxy: http://127.0.0.1:9030
    checkEndpoint: /health
    ttl: 0
groups:
  resident:
    swap: false
    exclusive: false
    persistent: true
    members:
    - laguna-s-2.1
