# CLAUDE.md

This file exists so Claude Code auto-loads the agent briefing when a session
starts in this repo. The briefing itself lives in `AGENTS.md` (the cross-tool
convention) — keep the content there, not here, so every agent reads one copy.

@AGENTS.md
